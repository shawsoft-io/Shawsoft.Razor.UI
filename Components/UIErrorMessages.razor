@using Microsoft.AspNetCore.Components.Forms

@if (Error != default)
{
    <UIAlertBox 
        Type="AlertType.Warning" 
        Title="Application Error" >
        <p>@(Error?.InnerException?.Message ?? Error?.Message)</p>
    </UIAlertBox>
}



@code {
    [Parameter]
    public Exception? Error { get; set; }
}